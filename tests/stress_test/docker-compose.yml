name: stresstest
services:
  tester:
    container_name: tester
    build: ./tester
    volumes:
      - ./tester:/data
    networks:
      - test-network
  
  server:
    container_name: server
    ports:
      - "9090:9090"
    build: ./server
    volumes:
      - /Users/maximeturgeon/42_cursus/webserv:/webserv/ressources
    networks:
      - test-network


networks:
  test-network:
    driver: bridge
